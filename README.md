# Deep-Learning-With-Python

A study on Deep learning with Python based on notes by Francois Chollet

## Setup
To set up the environment, run the following command:
`pipenv shell`  
then
`pipenv install` 
